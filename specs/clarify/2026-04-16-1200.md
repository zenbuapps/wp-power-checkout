# Clarify Session 2026-04-16 12:00

## Idea

啟用 Shopline Payment LINE Pay 付款方式。解除前端 UI disabled 限制、調整警告訊息、更新後端預設值、確認退款規則。

確定的部分：
1. 前端解禁：types.ts 中 LINE Pay 的 disabled: true → false，移除 tooltip
2. 警告調整：index.vue 中 LINE Pay alert 從 type="error" 改為 type="warning"，文案改為「請先確認已在 SLP 後台啟用 LINE Pay」
3. 後端預設值：RedirectSettingsDTO.php 取消 'LinePay' 註解
4. 向下相容：既有商店升級後 LINE Pay 不會自動啟用
5. WC Block Checkout：LINE Pay 作為 shopline_payment_redirect gateway 的一部分自動支援，不需要新的 Block entry
6. PaymentMethodOptions：根據 SLP API 文件，LINE Pay 不需要 paymentMethodOptions

待確認：
1. LINE Pay 退款能力（部分退款？全額退款？）
2. PaymentMethodOptions 註解更新
3. Sandbox 測試帳號是否已開通 LINE Pay
4. CLAUDE.md 退款表格是否需要同步更新

## Q&A

- Q: LINE Pay 退款規則（全額+部分 / 僅全額 / 不支援）？ → A: 支援全額 + 部分退款，與信用卡相同，維持目前 can_refund() 邏輯不變
- Q: PaymentMethodOptions 註解是否需要更新？ → A: (auto-resolved) 將「暫不支援設定」改為「不需要設定」，更精確描述事實
- Q: Sandbox 測試帳號是否已開通 LINE Pay？ → A: (auto-resolved) SLP sandbox 非信用卡付款方式統一導轉至模擬頁面，不需額外開通
- Q: CLAUDE.md 退款表格是否需要同步更新？ → A: (auto-resolved) 需要新增 LINE Pay 列：Partial Refund=Yes, Full Refund=Yes
